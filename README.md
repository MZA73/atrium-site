# ATRIUM - site (Next.js, version a plat)

Projet Next.js (Pages Router) sans sous-dossiers imbriques, pour un import GitHub par glisser-deposer fiable.

## Local
npm install puis npm run dev (http://localhost:3000)

## Deploiement
Vercel importe le depot ; domaine atrium.templeimmo.com via CNAME chez OVH.
